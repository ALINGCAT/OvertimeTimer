# 会话恢复点

## 当前目标

为 WPF 加班记录应用搭建框架，并按设计文档持续推进功能。

## 已完成

- 已生成 `design` 目录及拆分文档。
- 已生成 `TODO` 目录及任务清单。
- 已补充开发规范：
  - UI 任务不得阻塞
  - 异步统一返回 `Task`
  - 不使用 `async void`
- 已生成基础 WPF 解决方案：
  - `OvertimeTimer.sln`
  - `src/OvertimeTimer.App`
  - `src/OvertimeTimer.Core`
- 已为主项目和核心库建立基础目录：
  - `Views`
  - `ViewModels`
  - `Services`
  - `Models`
- 已接入 Prism 和 HandyControls。
- 已完成主界面基础布局、月历展示和日记/加班记录面板。
- 已实现年月修改弹窗，支持年/月下拉框和“回到今天”。
- 已将主界面年月修改入口收缩为小铅笔按钮。
- 已将设置改为独立窗口，通过 `ShowDialog()` 打开并阻塞主窗口。
- 已将设置入口放到月历标题左侧，使用齿轮按钮。
- 已完成当前选择日期显示，且“今天”操作会同步切换到当天。
- 已补充开发规范：XAML 间距优先使用 `Margin`，不使用空 `Grid` 行/列分隔。
- 已新增项目主目录规范文件 `PROJECT_RULES.md`，后续按其约束执行布局与异步实现。
- 已将设置页重构为左侧分类页签 + 右侧内容区结构。
- 已完成设置页共享页签按钮样式，并统一页签视觉。
- 已接入“工作日规则 / 存储设置 / 语言设置”三类设置页签，其中语言设置仍为占位。
- 已完成存储设置基础表单，支持路径输入、目录选择弹窗、存储方式切换。
- 已实现保存存储设置前的路径校验；路径不存在时可确认后自动创建目录。
- 已将设置页保存反馈改为页内显示，位置在保存按钮右侧，并于 3 秒后自动消失。
- 已完成按天模式布局调整：锚点天数独立成行，右侧显示今天日期。
- 已限制“今天是循环的第 N 天”必须处于 `1 ~ (工作天数 + 休息天数)`，越界时自动收敛。
- 已实现 `settings.json` 的异步读写，保存路径为 `%AppData%\\OvertimeTimer\\settings.json`。
- 已新增外观设置分类，支持配置窗口背景色、月历工作日/休息日/今日/非本月文字色，以及加班/日记提示点颜色。
- 已去除休息日加班字体变红的旧逻辑，统一使用外观设置中的颜色资源。
- 已将外观设置纳入 `settings.json` 持久化，并在应用启动时自动恢复并应用。
- 已将设置页按分类拆分为独立视图文件：工作日规则、存储设置、外观设置、语言设置分别维护。
- 已将设置逻辑按分类拆分为独立子 ViewModel，主 `SettingsViewModel` 只负责页签切换、装配和统一保存入口。
- 已将设置持久化编排拆分为独立协调服务，主 `SettingsViewModel` 不再直接操作 `SettingsDataStore`。
- 已将月历单元的悬停/选中样式抽成共享资源，减少首次交互时的样式实例化开销。
- 已实现设置页启动加载与手动保存恢复。
- 已实现“当前周序号”随“周期次数”自动校验与收敛。
- 已完成本次构建验证，`dotnet build` 通过。

## 当前代码状态

- 主项目可编译，`dotnet build` 已通过。
- `MainView` 使用自定义月历网格，左侧为日历与当日记录摘要，右侧为 Markdown 日记与预览。
- `MonthPickerWindow` 已改为下拉框选择年月，窗口内提供“今天”。
- `SettingsWindow` 已成为独立窗口。
- `SettingsView` 已支持分类页签切换，工作日规则与存储设置可编辑，语言设置为占位。
- 存储设置支持目录选择、路径存在性校验、确认创建目录和页内保存反馈。
- 工作日按天模式中，“今天是循环的第 N 天”已与工作/休息天数联动约束。
- 设置页当前可将工作日规则与存储设置持久化到 `settings.json`，并在下次启动时恢复。
- `App.xaml` 中已集中放置主题资源与 `BoolToVisibility`。
- `App.xaml` 中已集中放置主题资源、设置页签样式与可动态更新的颜色刷。
- 主窗口顶部菜单已移除。

## 关键约束

- 所有行为和任务都不能阻塞 UI。
- 所有异步接口统一返回 `Task`。
- 不允许 `async void`，除非框架强制事件入口且已最小封装。
- 中文、英文为内置语言，其他语言通过用户语言包扩展。
- 日记支持可配置存储路径与分组方式。
- 切换月份时需要预加载当月加班记录与日记并同步 UI。
- 月历样式需要保持紧凑，格子宽度、间距、悬停和选中状态需保持统一。
- XAML 布局优先使用 `Margin` 控制间距，不使用空行/空列分隔。
- `View` 主体优先使用 `Grid`，尽量不用 `StackPanel`；子控件可酌情使用。

## 日记存储规则

- 文件命名统一为 `年-月-日.md`。
- 按年分类示例：`存储路径/年份/年-月-日.md`
- 按月分类示例：`存储路径/年份-月份/年-月-日.md`
- 默认路径为应用目录下的 `dailies`
- 默认路径不存在时自动创建
- 选中已有日记日期时自动加载
- 保存覆盖当天文件内容
- `Ctrl+S` 快速保存
- 保存后状态栏提示“日记已保存”

## 待继续事项

- 完成单日记录的加载、保存覆盖和月切换同步刷新。
- 补齐输入校验、Ctrl+S 保存和状态栏反馈。
- 完成 records.json、本地日记文件与月历状态的持久化联动。
- 完成本地化资源与语言包机制。
- 继续完善设置页与工作日规则配置，包括语言设置内容、自动保存与更完整的异常处理。
- 后续继续按当前 UI 结构推进，不再回退到顶部菜单布局。

## 备注

- 曾尝试将 `D:\快速访问` 重命名为英文目录名 `QuickAccess`，但当前路径被占用，重命名未成功。
- 如果下次继续，建议先关闭占用该目录的终端/程序后再重试。

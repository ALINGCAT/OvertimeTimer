# 项目规范

本文件用于约束后续在本项目中的实现与修改。若与会话中的临时记忆不一致，以本文件为准。

## 布局规范

- 不允许使用 `Grid` 的空白行或空白列作为间隔留空。
- 如需间隔，优先通过子控件的 `Margin` 实现。
- `View` 主体优先使用 `Grid` 排列。
- 尽量避免在 `View` 主体使用 `StackPanel`。
- 子控件可以按需、酌情使用 `StackPanel`。

## 异步规范

- 不允许直接使用 `async void`。
- 所有异步方法统一返回 `Task`。
- 仅在框架强制的事件入口场景下，才允许最小化封装，并优先将实际逻辑下沉到 `Task` 方法中。

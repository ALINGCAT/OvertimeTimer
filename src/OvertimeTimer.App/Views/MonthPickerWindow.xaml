<Window x:Class="OvertimeTimer.App.Views.MonthPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:design="clr-namespace:OvertimeTimer.App.DesignTime"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="选择年月"
        d:DataContext="{d:DesignInstance Type=design:DesignMonthPickerViewModel, IsDesignTimeCreatable=True}"
        Width="220"
        Height="120"
        Background="{DynamicResource AppWindowBackgroundBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <ComboBox Width="72"
                          ItemsSource="{Binding YearOptions}"
                          SelectedItem="{Binding SelectedYear, Mode=TwoWay}"
                          Margin="0,0,8,0" />
                <TextBlock VerticalAlignment="Center" Text="年" />
            </StackPanel>

            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <ComboBox Width="56"
                          ItemsSource="{Binding MonthOptions}"
                          SelectedItem="{Binding SelectedMonth, Mode=TwoWay}"
                          Margin="0,0,8,0" />
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Right" Text="月" />
            </StackPanel>

            <Button Grid.Row="1"
                    Grid.Column="0"
                    Margin="0 5 0 0"
                    Content="今天"
                    HorizontalAlignment="Left"
                    Command="{Binding TodayCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
            <Button Grid.Row="1"
                    Grid.Column="1"
                    Margin="0 5 0 0"
                    Content="确定"
                    HorizontalAlignment="Right"
                    Command="{Binding ConfirmCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
        </Grid>
</Window>

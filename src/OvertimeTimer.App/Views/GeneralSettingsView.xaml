<UserControl x:Class="OvertimeTimer.App.Views.GeneralSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:loc="clr-namespace:OvertimeTimer.App.Localization"
             xmlns:vm="clr-namespace:OvertimeTimer.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:GeneralSettingsViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontWeight="SemiBold" Text="日记存储" />

        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{loc:Loc Settings.StorageRootPath}" />
            <TextBox Grid.Column="1" Margin="12,0,12,0" Text="{Binding StorageSection.DiaryRootPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="2" Command="{Binding StorageSection.ChooseDiaryRootPathCommand}" Content="{loc:Loc Settings.StorageChooseDir}" />
        </Grid>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <RadioButton HorizontalAlignment="Left" Content="{loc:Loc Settings.StorageFlat}" IsChecked="{Binding StorageSection.IsFlatStorageMode}" />
            <RadioButton Margin="112,0,0,0" HorizontalAlignment="Left" Content="{loc:Loc Settings.StorageByYear}" IsChecked="{Binding StorageSection.IsByYearStorageMode}" />
            <RadioButton Margin="212,0,0,0" HorizontalAlignment="Left" Content="{loc:Loc Settings.StorageByMonth}" IsChecked="{Binding StorageSection.IsByMonthStorageMode}" />
        </Grid>

        <TextBlock Grid.Row="3" Margin="0,6,0,0" Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding StorageSection.DiaryStorageModeDescription}" TextWrapping="Wrap" />

        <Separator Grid.Row="4" Margin="0,16,0,4" />

        <TextBlock Grid.Row="5" FontWeight="SemiBold" Text="{loc:Loc Language.Language}" />

        <Grid Grid.Row="6" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" Width="140"
                      ItemsSource="{Binding AvailableLanguages}"
                      DisplayMemberPath="Name"
                      SelectedItem="{Binding SelectedLanguage}" />
        </Grid>

        <Grid Grid.Row="7" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" HorizontalAlignment="Left" Command="{Binding SaveCommand}" Content="{loc:Loc Settings.Save}" />
            <Button Grid.Column="1" Margin="8,0,0,0" HorizontalAlignment="Left" Command="{Binding OpenSettingsDirectoryCommand}" Content="{loc:Loc Settings.OpenConfigDir}" />
            <TextBlock Grid.Column="2"
                       Style="{StaticResource InlineSaveFeedbackTextStyle}"
                       Visibility="{Binding HasSaveFeedback, Converter={StaticResource BoolToVisibility}}"
                       Text="{Binding SaveFeedbackMessage}" />
        </Grid>
    </Grid>
</UserControl>

<UserControl x:Class="OvertimeTimer.App.Views.StorageSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:loc="clr-namespace:OvertimeTimer.App.Localization"
             xmlns:vm="clr-namespace:OvertimeTimer.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:StorageSettingsViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{loc:Loc Settings.StorageRootPath}" />
            <TextBox Grid.Column="1" Margin="12,0,12,0" Text="{Binding DiaryRootPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="2" Command="{Binding ChooseDiaryRootPathCommand}" Content="{loc:Loc Settings.StorageChooseDir}" />
        </Grid>

        <Grid Grid.Row="1" Margin="0,12,0,0">
            <RadioButton HorizontalAlignment="Left" Content="{loc:Loc Settings.StorageFlat}" IsChecked="{Binding IsFlatStorageMode}" />
            <RadioButton Margin="112,0,0,0" HorizontalAlignment="Left" Content="{loc:Loc Settings.StorageByYear}" IsChecked="{Binding IsByYearStorageMode}" />
            <RadioButton Margin="212,0,0,0" HorizontalAlignment="Left" Content="{loc:Loc Settings.StorageByMonth}" IsChecked="{Binding IsByMonthStorageMode}" />
        </Grid>

        <TextBlock Grid.Row="2" Margin="0,12,0,0" Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding DiaryStorageModeDescription}" TextWrapping="Wrap" />

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" HorizontalAlignment="Left" Command="{Binding SaveCommand}" Content="{loc:Loc Settings.Save}" />
            <TextBlock Grid.Column="1"
                       Style="{StaticResource InlineSaveFeedbackTextStyle}"
                       Visibility="{Binding HasSaveFeedback, Converter={StaticResource BoolToVisibility}}"
                       Text="{Binding SaveFeedbackMessage}" />
        </Grid>
    </Grid>
</UserControl>

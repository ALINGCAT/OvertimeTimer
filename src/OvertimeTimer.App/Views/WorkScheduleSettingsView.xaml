<UserControl x:Class="OvertimeTimer.App.Views.WorkScheduleSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:loc="clr-namespace:OvertimeTimer.App.Localization"
             xmlns:vm="clr-namespace:OvertimeTimer.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:WorkScheduleSettingsViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <RadioButton HorizontalAlignment="Left" Content="{loc:Loc Settings.Weekly}" IsChecked="{Binding IsWeeklyMode}" />
            <RadioButton Margin="92,0,0,0" HorizontalAlignment="Left" Content="{loc:Loc Settings.Daily}" IsChecked="{Binding IsDailyMode}" />
        </Grid>

        <Grid Grid.Row="1"
              Margin="0,12,0,0"
              Visibility="{Binding IsWeeklyMode, Converter={StaticResource BoolToVisibility}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{loc:Loc Settings.CycleCount}" />
                <hc:NumericUpDown Grid.Column="1" Margin="8,0,24,0" Minimum="1" Maximum="52" Value="{Binding WeekCycleCount}" />
                <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="{loc:Loc Settings.CurrentWeekIndex}" />
                <hc:NumericUpDown Grid.Column="3" Margin="8,0,0,0" Minimum="1" Maximum="52" Value="{Binding CurrentCycleWeekIndex}" />
            </Grid>

            <ItemsControl Grid.Row="1" Margin="0,12,0,0" ItemsSource="{Binding WeeklyCycleItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Padding="12" BorderBrush="{DynamicResource ShellBorderBrush}" BorderThickness="1" CornerRadius="8">
                            <Grid>
                                <TextBlock FontWeight="SemiBold" Text="{Binding WeekLabel}" />
                                <WrapPanel Margin="0,24,0,0">
                                    <CheckBox Margin="0,0,12,8" Content="{loc:Loc Calendar.Monday}" IsChecked="{Binding MondayWork}" />
                                    <CheckBox Margin="0,0,12,8" Content="{loc:Loc Calendar.Tuesday}" IsChecked="{Binding TuesdayWork}" />
                                    <CheckBox Margin="0,0,12,8" Content="{loc:Loc Calendar.Wednesday}" IsChecked="{Binding WednesdayWork}" />
                                    <CheckBox Margin="0,0,12,8" Content="{loc:Loc Calendar.Thursday}" IsChecked="{Binding ThursdayWork}" />
                                    <CheckBox Margin="0,0,12,8" Content="{loc:Loc Calendar.Friday}" IsChecked="{Binding FridayWork}" />
                                    <CheckBox Margin="0,0,12,8" Content="{loc:Loc Calendar.Saturday}" IsChecked="{Binding SaturdayWork}" />
                                    <CheckBox Margin="0,0,12,8" Content="{loc:Loc Calendar.Sunday}" IsChecked="{Binding SundayWork}" />
                                </WrapPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <Grid Grid.Row="2"
              Margin="0,12,0,0"
              Visibility="{Binding IsDailyMode, Converter={StaticResource BoolToVisibility}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="80" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{loc:Loc Settings.WorkDays}" />
                <hc:NumericUpDown Grid.Column="1" Margin="8,0,24,0" Minimum="1" Maximum="365" Value="{Binding WorkDays}" />
                <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="{loc:Loc Settings.RestDays}" />
                <hc:NumericUpDown Grid.Column="3" Margin="8,0,24,0" Minimum="0" Maximum="365" Value="{Binding RestDays}" />
            </Grid>

            <Grid Grid.Row="1" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Text="{loc:Loc Settings.TodayIsCycleDay}" />
                <hc:NumericUpDown Grid.Column="1" Margin="8,0,8,0" Minimum="1" Maximum="{Binding AnchorWorkDayIndexMax}" Value="{Binding AnchorWorkDayIndex}" />
                <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="{loc:Loc Settings.DayUnit}" />
                <TextBlock Grid.Column="3" Margin="12,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding TodayDescription}" />
            </Grid>
        </Grid>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" HorizontalAlignment="Left" Command="{Binding SaveCommand}" Content="{loc:Loc Settings.Save}" />
            <TextBlock Grid.Column="1"
                       Style="{StaticResource InlineSaveFeedbackTextStyle}"
                       Visibility="{Binding HasSaveFeedback, Converter={StaticResource BoolToVisibility}}"
                       Text="{Binding SaveFeedbackMessage}" />
        </Grid>
    </Grid>
</UserControl>

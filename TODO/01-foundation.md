# 项目基础与框架 TODO

## 目标

搭建 WPF 应用的基本结构和 MVVM 骨架。
所有用户可见行为和任务都不得阻塞 UI。
所有异步接口统一返回 `Task`，不使用 `async void`。

## TODO

- [x] 创建 WPF 项目基础结构。
- [x] 选择并统一 MVVM 方案。
- [x] 建立主窗口布局骨架。
- [x] 建立页面/视图切换结构。
- [x] 建立基础命令和通知基类。
- [x] 规划资源字典、样式和主题入口。
- [x] 定义全局导航或视图切换方式。
- [x] 确保视图层事件处理不使用 `async void`。
- [ ] 预留本地化资源加载入口和语言切换入口。
- [ ] 设计可从外部语言包加载文本的扩展点，支持用户自行添加语言。
- [ ] 确保耗时操作通过异步任务在后台执行。

# 本地存储与恢复 TODO

## 目标

实现本地持久化、启动恢复和数据兼容。

## TODO

- [ ] 设计本地 JSON 文件结构。
- [ ] 实现 settings.json 读写。
- [ ] 实现 records.json 读写。
- [ ] 允许用户在设置里修改日记存储路径。
- [ ] 允许用户在设置里修改日记存储方式。
- [ ] 支持三种日记存储方式：不放入文件夹、按年分类、按月分类。
- [ ] 统一日记文件命名格式为 `年-月-日.md`。
- [ ] 按文件名直接定位某天的日记，忽略存储路径层级。
- [ ] 默认日记路径为应用目录下的 `dailies`。
- [ ] 若默认路径不存在则自动创建。
- [ ] 按年分类时保存到 `存储路径/年份/年-月-日.md`，其中年份目录名来自目标日期的年份。
- [ ] 按月分类时保存到 `存储路径/年份-月份/年-月-日.md`，其中月份目录名来自目标日期的年月。
- [ ] 切换月份时预读取该月所有加班记录与日记。
- [ ] 切换月份时刷新日历上的红点、绿点和休息日状态。
- [ ] 实现应用启动时的数据加载。
- [ ] 实现最近选中日期恢复。
- [ ] 实现记录变更后自动保存。
- [ ] 实现设置变更后自动保存。
- [ ] 实现临时文件写入与原子替换。
- [ ] 确保本地读写不会阻塞 UI 线程。
- [ ] 确保所有存储相关异步方法返回 `Task`。
- [ ] 预留后续迁移到 SQLite 的扩展点。
